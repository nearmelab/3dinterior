---
export interface Props {
  id: string;
  title: string;
}

const { id, title } = Astro.props;
---

<section id={id} class="scroll-mt-28">
  <h2 class="text-2xl font-bold mt-10 mb-4 text-slate-900 dark:text-white">{title}</h2>
  <slot />
</section>